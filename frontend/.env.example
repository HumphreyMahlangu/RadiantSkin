# Development and preview proxy /api to the existing Spring Boot routes.
API_PROXY_TARGET=http://127.0.0.1:8080

# Optional production API origin, without /api for the existing backend.
# Otherwise your host must proxy /api/* to the backend and strip /api.
# VITE_API_BASE_URL=https://your-api.example.com
